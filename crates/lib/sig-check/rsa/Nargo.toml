[package]
name = "sig_check_rsa"
type = "lib"
authors = ["Theo Madzou"]
compiler_version = ">=0.36.0"

[dependencies]
# rsa = { tag = "main", git = "https://github.com/zkpassport/noir_rsa.git", directory = "lib" }
# rsa = { tag = "mc/update-to-0.4.0", git = "https://github.com/iAmMichaelConnor/noir_rsa.git", directory = "lib" }
rsa = { path = "../../../../../noir_rsa/lib" } # https://github.com/iAmMichaelConnor/noir_rsa/blob/mc/update-to-0.4.0/lib/Nargo.toml but with a Nargo.toml which points the `main` branch of bignum.
# bignum = { tag = "main", git = "https://github.com/noir-lang/noir-bignum.git" }
bignum = { path = "../../../../../noir-bignum/"}
utils = { path = "../../utils" }