[package]
name = "sig_check_rsa"
type = "lib"
authors = ["Theo Madzou"]
compiler_version = ">=0.36.0"

[dependencies]
rsa = { git = "https://github.com/zkpassport/noir_rsa", tag = "main" }
bignum = { git = "https://github.com/noir-lang/noir-bignum", tag = "main"}
utils = { path = "../../utils" }
