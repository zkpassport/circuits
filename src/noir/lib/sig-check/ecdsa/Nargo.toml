[package]
name = "sig_check_ecdsa"
type = "lib"
authors = ["Theo Madzou"]
compiler_version = ">=0.35.0"

[dependencies]
utils = { path = "../../utils" }
ecdsa = { tag = "main", git = "https://github.com/zkpassport/noir-ecdsa" }
bignum = {tag = "main", git = "https://github.com/noir-lang/noir-bignum"}
bigcurve = {tag = "main", git = "https://github.com/madztheo/noir_bigcurve"}