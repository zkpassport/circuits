[package]
name = "csc_to_dsc_commitment"
type = "lib"
authors = ["Theo Madzou"]
compiler_version = ">=0.22.0"

[dependencies]
utils = { path = "../../utils" }
common = { path = "../common" }
merkle_tree = { git = "https://github.com/privacy-scaling-explorations/zk-kit.noir", tag = "zpedro/0.36.0", directory = "packages/merkle-trees" }